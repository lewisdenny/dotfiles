# Add ~/.zfunc into the fzsh function seatch path https://unix.stackexchange.com/a/33898
# fpath=( ~/.zfunc "${fpath[@]}" )
#
# autoload -Uz worktree-create
# autoload -Uz rdo-change
# autoload -Uz nodeset-check
# autoload -Uz change-since-last-review
# autoload -Uz check_static_pool_count
# autoload -Uz gh-pr-checkout
# autoload -Uz cd_and_nvim
# autoload -Uz gh-repo-clone

